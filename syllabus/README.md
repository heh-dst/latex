# Template pour documents LaTeX

* `document.tex` est le document principal.
  Il contient notamment les entêtes.
* `content.tex` est destiné à recevoir le corps du document.
  Il est inclus à partir de `document.tex`